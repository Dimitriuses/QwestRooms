{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",

      // Every statement the application sends, with its parameters and timings. This is how the
      // two-queries-per-page claim in the README can be checked by hand rather than taken on
      // trust: load the catalogue and count the "Executed DbCommand" lines.
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
