@model IReadOnlyList<CityDto>

@if (Model.Count == 0)
{
    <span class="dropdown-item-text text-body-secondary">No cities</span>
}

@foreach (var city in Model)
{
    <button type="button" class="dropdown-item js-city" data-id="@city.Id">@city.Name</button>
}
