@using QwestRooms.BLL.DTOModels

@model IReadOnlyList<CityDTO>

@if (Model.Count == 0)
{
    <span class="dropdown-item-text text-muted">No cities</span>
}

@foreach (var item in Model)
{
    <button type="button" class="dropdown-item js-city" data-id="@item.Id">@item.Name</button>
}
